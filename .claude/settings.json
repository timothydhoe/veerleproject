{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "if command -v python3 >/dev/null 2>&1; then PY=python3; else PY=python; fi; \"$PY\" \"${CLAUDE_PROJECT_DIR}/.claude/hooks/gdpr_guard.py\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "if command -v python3 >/dev/null 2>&1; then PY=python3; else PY=python; fi; \"$PY\" \"${CLAUDE_PROJECT_DIR}/.claude/hooks/config_guard.py\""
          },
          {
            "type": "command",
            "shell": "bash",
            "command": "if command -v python3 >/dev/null 2>&1; then PY=python3; else PY=python; fi; \"$PY\" \"${CLAUDE_PROJECT_DIR}/.claude/hooks/r_syntax_check.py\""
          }
        ]
      }
    ]
  }
}
